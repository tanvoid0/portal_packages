name: portal_crypto
description: AES-GCM encryption, key wrapping, and vault blob format for Portal apps.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.8.1 <4.0.0"

dependencies:
  cryptography: ^2.7.0
  meta: ^1.11.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
