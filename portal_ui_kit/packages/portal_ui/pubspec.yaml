name: portal_ui
description: Legacy package name re-exporting portal_ui_compat for existing imports.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  portal_ui_compat:
    path: ../portal_ui_compat

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
