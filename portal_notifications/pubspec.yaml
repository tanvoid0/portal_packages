name: portal_notifications
description: Shared local notification scheduling for Portal apps.
version: 0.1.0
repository: https://github.com/tanvoid0/pocket_portal/tree/main/packages/portal_notifications

environment:
  sdk: ">=3.8.1 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^18.0.1
  flutter_timezone: ^5.0.2
  shared_preferences: ^2.2.2
  timezone: ^0.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
